/**
 * virtuals are not saved in the dicument but calcluated based on the document and processed in response
 * schema.virtaul(nameOfVirtualProperty).get(function(){
 *  return 'some value'
 * })
 * Virtuals need to be explciitly truned on using optins in schema
 * */